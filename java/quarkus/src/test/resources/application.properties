# Test config: auth disabled so the yamux WS test client can connect without a
# Bearer key, and the tunnel mounts on the Quarkus test HTTP server (shared port).
tun-proto.enabled=true
tun-proto.auth-disabled=true
tun-proto.path=/tunnels

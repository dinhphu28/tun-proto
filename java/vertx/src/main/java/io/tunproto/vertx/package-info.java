/**
 * Embeddable Vert.x library for standing up a tun-proto reverse-TCP tunnel
 * server. The public surface is {@link io.tunproto.vertx.TunnelServer} and
 * {@link io.tunproto.vertx.TunnelOptions}; auth via
 * {@link io.tunproto.vertx.Authenticator} and egress control via
 * {@link io.tunproto.vertx.TargetPolicy}.
 */
package io.tunproto.vertx;
